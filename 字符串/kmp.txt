void getNext() {
    nxt[1] = 0;
    int j = 0, len = strlen(t + 1);
    for (int i = 2; i <= len; i++) {
        while (j && t[j + 1] != t[i]) j = nxt[j];
        if (t[j + 1] == t[i]) j++;
        nxt[i] = j;
    }
}


for (int i = 1; i <= N; i++) {
        while (j && t[j + 1] != s[i])
            j = nxt[j];
        if (t[j + 1] == s[i]) j++;
        if (j == M) {
            ans.push_back(i - M + 1);
            j = nxt[j];
        }
}